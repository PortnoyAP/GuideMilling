<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guidemilling">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.login.LoginActivity2"
            android:label="@string/title_activity_login2"></activity>
        <activity android:name=".tips.TipActivity" />
        <activity android:name=".tap.TapActivity" />
        <activity android:name=".cnc_calculator.CncCalculator" />
        <activity android:name=".m_code.McodeActivity" />
        <activity android:name=".g_code.GcodeActivity" />
        <activity android:name=".myTechnology.MyTechnologyResult" />
        <activity android:name=".MainMenu" />
        <activity android:name=".myTechnology.MyTechnologySelector" />
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>